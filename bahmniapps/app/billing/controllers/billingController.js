'use strict';

angular.module('bahmni.billing')
    .controller('billingController', ['$scope', '$state', 'appService', 'locationService', 'spinner', '$bahmniCookieStore', '$window', '$q', 'messagingService','billingService',
        function($scope, $state, appService, locationService, spinner, $bahmniCookieStore, $window, $q, messagingService, billingService) {



        var init = function() {
            //getParams();
        };


    /**
     * @author : Eric Mwailunga (UCC)
     * JAnuary, 2019
     * Creation of sale quotations
     **/
        var createSaleQuotation = function (){

        };



        init();
    }]);